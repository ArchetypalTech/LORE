// Area
pub fn unimplemented_text() -> Array<ByteArray> {
    array![
        "This is not ready yet",
    ]
}

// Exit
pub fn unmatchedName_text() -> Array<ByteArray> {
    array![
        "Does not exist",
        "No, that only exists in your mind",
        "What are you referring to?",
        "What do you mean?",
    ]
}

pub fn noMatchDirection_text() -> Array<ByteArray> {
    array![
        "No, wrong direction",
        "Can you even go there?",
        "You can't go there",
    ]
}

pub fn unenterable_text() -> Array<ByteArray> {
    array![
        "You can't go there",
        "It's not open",
        "It's closed",
        "It's unenterable",
    ]
}

// Reactable
pub fn failToReact_text() -> Array<ByteArray> {
    array![
        "I'm at a loss",
        "You're not helping",
        "You can't imagine things",
        "Can you repeat that?",
    ]
}

pub fn noTarget_text() -> Array<ByteArray> {
    array![
        "What are you trying to look at?",
        "Looking at what?",
        "You can't look at the nothingness",
    ]
}

// Container
pub fn notOpen_text() -> Array<ByteArray> {
    array![
        "It's not open",
        "It's closed",
    ]
}

pub fn containerFull_text() -> Array<ByteArray> {
    array![
        "It's alreadyfull",
        "You can't put anything more in there",
        "You can't store anything more",
        "Can't exceed capacity limit",
    ]
}

pub fn cantStore_text() -> Array<ByteArray> {
    array![
        "It can't store",
        "Is that even possible?",
        "Can't story anything here",
    ]
}

pub fn cantBePicked_text() -> Array<ByteArray> {
    array![
        "It can't be picked",
        "Its weight is something not from this world",
        "It won't budge in this lifetime",
    ]
}

pub fn cantBeStored_text() -> Array<ByteArray> {
    array![
        "It can't be stored",
        "It won't fit anywhere",
        "That won't happen",
    ]
}

pub fn alreadyStored_text() -> Array<ByteArray> {
    array![
        "It's already there",
        "You already have it",
        "You already have that",
    ]
}   

pub fn notStored_text() -> Array<ByteArray> {
    array![
        "It's not there",
        "You don't have it",
        "You don't have that",
    ]
}

pub fn noContainer_text() -> Array<ByteArray> {
    array![
        "That is not a container",
        "It's not a container",
        "It doesn't have an extradimensional space",
    ]
}

pub fn noPersonalContainer_text() -> Array<ByteArray> {
    array![
        "You don't have a personal container",
        "You are not a blackhole, therefore you can't do that",
        "You don't have magical pockets to do that"
    ]
}